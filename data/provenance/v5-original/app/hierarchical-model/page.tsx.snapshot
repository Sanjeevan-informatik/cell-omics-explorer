import { HierarchicalBiologyPanel } from "@/components/hierarchical-biology-panel";

export default function HierarchicalModelPage() {
  return (
    <main className="sequence-page-shell">
      <section className="sequence-page-content">
        <div className="sequence-page-intro">
          <div>
            <span className="eyebrow">Dedicated workspace</span>
            <h1>Hierarchical biological model</h1>
            <p>Sequence-centric layers remain together, small molecules remain separate, and the 30-base DNA example is linked to a 30-residue protein context with 2D and 3D views.</p>
          </div>
        </div>
        <HierarchicalBiologyPanel />
      </section>
    </main>
  );
}
